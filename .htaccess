# Nonaktifkan Directory Listing
Options -Indexes

# Redirect folder sensitif ke halaman error Cloudflare
RedirectMatch 302 ^/(vendor|storage|config|\.git|node_modules|tests|\.env|composer\.(json|lock)|package\.json)$ https://bpdaskonaweha.id/cf-403

# Izinkan hanya file HTML, CSS, JS, gambar, font
<FilesMatch "\.(html?|css|js|jpg|jpeg|png|gif|webp|ico|svg|woff2?|ttf|eot)$">
    Require all granted
</FilesMatch>

# Redirect semua file lainnya ke halaman error Cloudflare
<FilesMatch "^[^.]+$|.*\.(?!html?|css|js|jpg|jpeg|png|gif|webp|ico|svg|woff2?|ttf|eot)$">
    Redirect 302 / https://bpdaskonaweha.id/cf-403
</FilesMatch>